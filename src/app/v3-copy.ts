/**
 * 세 번째 판에서만 갈리는 문구
 *
 * 지금 쓰는 판의 문구(`V2_COPY`) 위에 이 표를 덮습니다 — 여기 적힌 열쇠만
 * 다른 글로 서고, 나머지는 그대로 지나갑니다.
 */

import { V2_COPY } from "@/app/v2-copy";
import type { CopyMap } from "@/components/copy";

export const V3_COPY: CopyMap = {
  ...V2_COPY,
};
